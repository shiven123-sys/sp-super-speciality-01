# TODO

- [x] Inspect current multi-page structure and identify theme disruption.
- [x] Use `style.css.bak` as the earlier theme source.
- [x] Restore earlier theme back into `style.css` without changing HTML structure.
- [x] Re-verify pages load correctly (index + pages/* + 404).
- [ ] If any CSS/JS mismatches remain, patch only the minimum styles/components required.


